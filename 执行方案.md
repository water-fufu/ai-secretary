# 秘书 2.0 执行方案

> 版本：1.0
> 创建日期：2026-09-14
> 架构：Electron 桌面客户端 + CloudBase 云端后端

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────┐
│              Electron 桌面客户端（本地）               │
│  ┌───────────────────────────────────────────────┐  │
│  │  Main Process（主进程）                        │  │
│  │  ├─ 窗口管理 / 系统托盘 / 全局快捷键 / 通知     │  │
│  │  └─ IPC 桥接（暴露安全 API 给渲染进程）         │  │
│  ├───────────────────────────────────────────────┤  │
│  │  Preload（预加载脚本）                         │  │
│  │  └─ contextBridge 暴露 window.mishu API       │  │
│  ├───────────────────────────────────────────────┤  │
│  │  Renderer（React UI）                          │  │
│  │  ├─ 聊天页 / 知识库页 / 会话管理 / 设置页       │  │
│  │  ├─ SSE 流式接收 / Markdown 渲染               │  │
│  │  └─ Zustand 状态管理                           │  │
│  └───────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────┘
                        │ HTTPS + SSE
                        ▼
┌─────────────────────────────────────────────────────┐
│          CloudBase 云托管（云端）                     │
│  ┌───────────────────────────────────────────────┐  │
│  │  FastAPI 后端（Docker 容器，端口 8080）         │  │
│  │  ├─ API 路由 / 业务服务 / RAG 检索             │  │
│  │  ├─ SQLAlchemy 异步 ORM                       │  │
│  │  ├─ Redis 缓存（精确+语义两层）                │  │
│  │  └─ FAISS 向量索引（持久化到云存储卷）          │  │
│  ├───────────────┬───────────────────────────────┤  │
│  │   MySQL 8.0   │   Redis 7                     │  │
│  └───────────────┴───────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## 二、技术栈

| 层级 | 技术 |
|------|------|
| 客户端框架 | Electron 30+ |
| 构建工具 | Vite + vite-plugin-electron |
| 前端 | React 18 + TypeScript |
| UI | Tailwind CSS + shadcn/ui |
| 状态管理 | Zustand |
| HTTP | axios |
| 流式输出 | SSE (fetch + ReadableStream) |
| Markdown | react-markdown + remark-gfm |
| 打包 | electron-builder + NSIS |
| 后端 | FastAPI |
| ORM | SQLAlchemy 2.0 + asyncmy |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7 |
| 向量库 | FAISS（持久化） |
| 部署 | CloudBase 云托管（Docker） |

---

## 三、项目目录结构

```
秘书2.0/
├── 执行方案.md                    # 本文档
├── README.md
├── .gitignore
│
├── client/                        # Electron 客户端
│   ├── package.json
│   ├── tsconfig.json
│   ├── tsconfig.node.json
│   ├── vite.config.ts
│   ├── electron-builder.yml
│   ├── index.html
│   ├── resources/                 # 图标
│   ├── electron/                  # 主进程
│   │   ├── main.ts
│   │   ├── preload.ts
│   │   ├── tray.ts
│   │   └── ipc/
│   └── src/                       # React 渲染进程
│       ├── api/
│       ├── components/
│       ├── hooks/
│       ├── store/
│       ├── types/
│       └── utils/
│
└── server/                        # FastAPI 后端
    ├── Dockerfile
    ├── requirements.txt
    ├── alembic.ini
    ├── alembic/
    └── app/
        ├── main.py
        ├── config.py
        ├── database.py
        ├── dependencies.py
        ├── models/
        ├── schemas/
        ├── api/
        ├── services/
        ├── rag/
        ├── core/
        └── tests/
```

---

## 四、Electron 客户端设计

### 4.1 主进程职责
- 创建无边框窗口，自定义标题栏
- 系统托盘：关闭时最小化到托盘，不退出
- 全局快捷键：Ctrl+Alt+M 唤起/隐藏
- 桌面通知：写入完成、冲突检测提醒
- 本地配置存储：API 地址等

### 4.2 Preload 安全桥接
通过 contextBridge 暴露有限 API：
- window.mishu.minimize() / close()
- window.mishu.notify(title, body)
- window.mishu.getConfig() / setConfig(cfg)

### 4.3 SSE 流式接收
使用 fetch + ReadableStream（POST 支持），解析 SSE 事件：
- status / sources / conflict / token / done / error

### 4.4 打包配置
electron-builder + NSIS：
- 非一键安装，允许选择目录
- 创建桌面快捷方式和开始菜单
- 产品名：秘书

---

## 五、CloudBase 后端设计

### 5.1 Dockerfile
```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PORT=8080
EXPOSE 8080
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080", "--workers", "2"]
```

### 5.2 配置（环境变量）
- DATABASE_URL: MySQL 连接串
- REDIS_URL: Redis 连接串
- DEEPSEEK_API_KEY: LLM 密钥
- FAISS_INDEX_PATH: 向量索引持久化路径
- CORS_ORIGINS: 跨域来源

---

## 六、API 接口清单

| 方法 | 路径 | 功能 | 流式 |
|------|------|------|------|
| GET | /api/v1/health | 健康检查 | 否 |
| POST | /api/v1/chat | 通用聊天 | SSE |
| POST | /api/v1/chat/qa | 知识库问答 | SSE |
| POST | /api/v1/chat/plan | 生成工作计划 | SSE |
| POST | /api/v1/chat/claude | 生成 Claude 指令 | SSE |
| POST | /api/v1/write/preview | 写入预览 | 否 |
| POST | /api/v1/write/confirm | 确认写入 | 否 |
| GET | /api/v1/vault/overview | 知识库概览 | 否 |
| POST | /api/v1/vault/refresh | 重建索引 | 否 |
| GET | /api/v1/notes | 笔记列表 | 否 |
| GET | /api/v1/notes/{id} | 笔记详情 | 否 |
| POST | /api/v1/notes | 新建笔记 | 否 |
| PUT | /api/v1/notes/{id} | 更新笔记 | 否 |
| DELETE | /api/v1/notes/{id} | 删除笔记 | 否 |
| GET | /api/v1/conversations | 会话列表 | 否 |
| POST | /api/v1/conversations | 新建会话 | 否 |
| GET | /api/v1/conversations/{id}/messages | 消息历史 | 否 |
| DELETE | /api/v1/conversations/{id} | 删除会话 | 否 |

---

## 七、数据库设计

### notes 笔记表
- id, folder, file_name, title, content(MEDIUMTEXT), source, is_archived, created_at, updated_at
- 唯一索引：(folder, file_name)

### chunks 切片表
- id, note_id(FK), section, content(TEXT), chunk_index, timestamp, created_at
- 索引：note_id

### conversations 会话表
- id, title, mode, created_at, updated_at

### messages 消息表
- id, conversation_id(FK), role, content(MEDIUMTEXT), mode, sources(JSON), has_conflict, conflict_summary, tokens_used, latency_ms, created_at

---

## 八、Redis 缓存设计

- 精确缓存：cache:exact:{sha256(question)}，TTL 3600s
- 语义缓存：cache:semantic:{embedding_id}，TTL 7200s
- 写入预览：write:pending:{conversation_id}，TTL 1800s
- 限流：rate_limit:{ip}，TTL 60s

---

## 九、实施阶段与验收标准

### P1: Electron 客户端脚手架
- [ ] Electron + Vite + React + TS + Tailwind 骨架
- [ ] 自定义标题栏（拖拽、最小化、关闭）
- [ ] electron-builder 配置，能打包出 NSIS 安装包
- [ ] 验收：双击安装 → 桌面快捷方式 → 打开窗口看到"秘书"界面

### P2: 客户端桌面特性
- [ ] 系统托盘（图标、右键菜单、单击显示/隐藏）
- [ ] 全局快捷键 Ctrl+Alt+M
- [ ] 桌面通知
- [ ] 关闭窗口最小化到托盘
- [ ] 本地配置存储（electron-store）
- [ ] 验收：快捷键唤起/隐藏，关闭后托盘仍在，通知弹出

### P3: 后端脚手架
- [ ] FastAPI 项目结构 + config + database
- [ ] SQLAlchemy 模型 + Alembic 迁移
- [ ] MySQL + Redis 连接
- [ ] Dockerfile（CloudBase 规范，端口 8080）
- [ ] /api/v1/health 健康检查
- [ ] 验收：docker build 成功，health 返回 200

### P4: RAG 核心 + 业务链路
- [ ] 文档切片（MarkdownHeaderTextSplitter）
- [ ] 向量化（all-MiniLM-L6-v2）
- [ ] FAISS 持久化（save_local/load_local）
- [ ] 检索 + 冲突检测
- [ ] 四条业务链路（问答/计划/Claude/写入）
- [ ] SSE 流式输出
- [ ] Redis 两层缓存
- [ ] 验收：curl 调 /api/v1/chat 收到 SSE 流，回答正确

### P5: 客户端功能页
- [ ] 聊天页（SSE 流 + Markdown 渲染 + 来源标注）
- [ ] 会话管理（列表、新建、删除、历史消息）
- [ ] 知识库页（概览、笔记列表、详情）
- [ ] 写入流程（预览卡片 + 确认按钮）
- [ ] 设置页（API 地址配置，默认留空）
- [ ] 模式选择器
- [ ] 验收：配置 API 地址后完整聊天、写入、查看知识库

### P6: 收尾
- [ ] 数据迁移脚本（从 1.0 tian_shu_vault 导入）
- [ ] README 部署指南（CloudBase 部署步骤）
- [ ] 统一异常处理 + 结构化日志
- [ ] 单元测试（核心函数）
- [ ] 验收：文档齐全，1.0 数据可导入，安装包可正常使用

---

## 十、关键决策记录

1. 客户端纯 UI 壳，不含 Python 后端，所有逻辑走云端 API
2. SSE 用 fetch + ReadableStream 而非 EventSource（需要 POST）
3. FAISS 持久化文件挂载 CloudBase 存储卷，不存 MySQL
4. Electron 安全：contextIsolation=true, nodeIntegration=false
5. 全局快捷键：Ctrl+Alt+M
6. 客户端主题：沿用薄荷绿
7. API 地址默认留空，用户在设置页配置
8. 开机自启：不做
